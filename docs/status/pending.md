# Pending Tasks

No hay pendientes críticos abiertos para la entrega actual.

## Limitaciones conocidas

- No existe `lint` porque el repositorio todavía no tiene configuración ESLint acordada.
- El build del frontend mantiene un warning de chunk grande en una ruta lazy del dashboard.
- Los tests de dashboard emiten warnings de tamaño de contenedor de Recharts en JSDOM, pero no rompen la suite.
